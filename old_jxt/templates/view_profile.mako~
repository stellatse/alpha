<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8" />
	<link rel="stylesheet" href="${request.static_url('servicemanager:static/style.css')}" type="text/css" media="screen" charset="utf-8" />
	<title>Service Manager Lite</title>
</head>
<body>
<div class="logo">
		<a href="">Service Manager Lite -Core</a>
		</div>
		<div class="nav" id="maia-nav-x">
				<div class="tab">
				<div class="logout">
				<a href="/logout">Logout</a>
				</div>
				
				<ul>
				<li class="p1"><a href="/todo">服务台</a></li>
				<li class="p2"><a href="/config">配置管理</a></li>
				<li class="p3"><a href="/change">变更管理</a></li>
				<li class="p4"><a href="/problem">问题管理</a></li>
				<li class="p5"><a href="/help">帮助</a></li>
				<ul>
				</div>
		</div>	
			
			
			<div class="main">
			<div class="welcome">
				<ul>
				<li class="p1"><a href="/config/user" style="font-weight:bold;">用户配置</a></li>
				<li class="p2"><a href="/config/device">设备管理</a></li>
				<li class="p3"><a href="/config/service">服务类别</a></li>
				<li class="p4"><a href="/config/ut" >Config Group</a></li>
				<ul>
				
			</div>
			<div class="configuration">
				<h2>用户配置管理中心</h2>
				<div class="content">
			<h1>User ID: ${profile.id}</h1>
			<table class="info_baseinfo">
			<tbody>
			% if profile:
			<form action="${request.route_url('view_user', uid=profile.id)}" method = "post" enctype="multipart/form-data">
                    % if message:
					<p style="color:red">${message}</p>
					% endif
					<label for="email">Email</label>
					<input name="email" type="text" value="${profile.email}" readonly/>
					<label for="name">用户名：</label>
					<input name="name" type="text" value="${profile.name}" readonly/>
					<label for="fullname">姓名：</label>
					<input name="fullname" type="text" value="${profile.fullname}" readonly/>
					
					<label for="eid">员工号</label>
					<input name="eid" type="text" value="${profile.eid}" readonly/>
					<label for="location">工位：</label>
					<input name="address" type="text" value="${profile.address}" readonly/>
					<label for="telephone">联系电话：</label>
					<input name="tele" type="text" value="${profile.telephone}" readonly/>
					<label for="type">用户类型：</label>
					<input name="type" type="text" value="${profile.usertype_id}" readonly/>
					<label>所属组</label>
					% for group in profile.mygroups:
					<a href="/user/${profile.id}/del/${group.id}">${group.name}<img src="${request.static_url('servicemanager:static/img/del.png')}" style="width:10px;height:10px"/></a>
					<br/>
					
					% endfor
					<label>添加所属组：</label>
						<select name="configgp">
						<option value="1">superuser</option>
						<option value="2">default</option>
						<option value="3">ChangeCordinator</option>
						<option value="4">ProblemCordinator</option>
						<option value="5">ServiceDeskHelp</option>
					</select>

			
			<input type="submit" name="chg.submitted" value="Append Group"/>
			</form>
			
			% endif
			</tbody>
			</table>
			</div>
				
				
				
				
			</div>
			<div class="foot">
				<p>&copy;2012-2012 Stella Xie All rights reserved.
					<a href="/">Contact us</a>
				</p>
			</div>
		</div>
		
</body>
</html>
